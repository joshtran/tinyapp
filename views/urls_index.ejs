<!DOCTYPE html>

<html lang="en">

<head>
  <title>TinyApp</title>
</head>

<body>
  <header>
    <% include ../views/partials/_header %>
  </header>

  <h1> URLs & Shortened forms</h1>

    <ul>
      <% for (var key in urls) { %>
        <li><%= key %> : <%= urls[key] %></li>
        <a href="http://localhost:8080/urls/<%= key %>">Edit</a>
        <form method="POST" action="/urls/<%= key %>/delete" style="margin-bottom: 20px;">
          <input type="submit" value="Delete">
        </form>
      <% }; %>

    </ul>

    <a href="http://localhost:8080/urls/new">Link to shorten a new URL</a>


</body>
</html>